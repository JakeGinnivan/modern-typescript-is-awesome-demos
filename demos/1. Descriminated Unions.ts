const LOAD_DATA = 'LOAD_DATA'
type LOAD_DATA = {
    type: LOAD_DATA
}

type Action = LOAD_DATA

type State = {

}

const reducer = (state: State, action: LOAD_DATA) => {
    switch (action.type) {
        default:
            state
    }
}